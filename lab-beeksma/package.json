{
  "name": "lab-beeksma",
  "version": "1.0.0",
  "description": "- The logData function accepts an array of files.  \r - It will read those files in the order they are given, and console log the first 8 bytes in the file.\r - Any files to be read must be in the assets folder.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "start": "node .",
    "lint": "eslint ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cowsay": "^1.1.9"
  },
  "devDependencies": {
    "eslint": "^4.5.0"
  }
}
